version: '3'

tasks:
  build:
    desc: Build the helloworld binary
    method: checksum
    sources:
      - go.*
      - '**/*.go'
    generates:
      - bin/helloworld
    cmds:
      - go run ./cmd/task-cache --task build -- go build -o bin/helloworld .
